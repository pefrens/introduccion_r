---
title: "tidyr - Organizacion de datos (Tidy Data)"
subtitle: "<span class='code'>Sesion 4</span>"
image: "images/04-theming.png"
tbl-colwidths: [5,20,75]
---

```{=html}
<iframe class="slide-deck" src="../materials/04-tidying-data.html" height="420" width="747" style="border: 1px solid #2e3846;"></iframe>
```

<a href='../materials/04-tidying-data.html' target="_blank">Ver diapositivas en pantalla completa</a>

# Esquema

- Principios de tidy data
- `pivot_longer()` y `pivot_wider()`
- `separate()` y `unite()`
- Flujo de limpieza previo al analisis

# Objetivo de la sesion

Transformar tablas entre formato ancho y largo de forma consistente para analisis y visualizacion.

# Datos usados

- Datos creados en memoria con `tibble()`

# Practica guiada

```r
library(tidyverse)
library(tidyr)
```

```r
datos_ancho <- tibble(
  departamento = c("CUSCO", "LIMA", "AREQUIPA"),
  pob_2022 = c(1350, 9700, 1500),
  pob_2023 = c(1380, 9850, 1530)
)

datos_ancho
```

```r
datos_largo <- datos_ancho |>
  pivot_longer(
    cols = starts_with("pob_"),
    names_to = "anio",
    values_to = "poblacion",
    names_prefix = "pob_"
  )

datos_largo
```

```r
datos_largo |>
  pivot_wider(
    names_from = anio,
    values_from = poblacion,
    names_prefix = "pob_"
  )
```

```r
ids <- tibble(codigo = c("08-01-01", "15-01-01"))

ids |>
  separate(codigo, into = c("ccdd", "ccpp", "ccdi"), sep = "-")
```

# Ejercicios

1. Convertir un dataset de meses en columnas a formato largo.
2. Regresar el resultado a formato ancho con `pivot_wider()`.
3. Separar un identificador compuesto y volver a unirlo con `unite()`.

# Resultado esperado

Preparar datos en formato tidy con transformaciones reproducibles.
